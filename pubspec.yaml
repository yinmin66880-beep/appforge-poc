name: anti_stay_up_late
description: 防熬夜锁机应用 - AppForge PoC 验证项目
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.0
  flutter_local_notifications: ^17.0.0
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
